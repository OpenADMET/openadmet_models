name: openadmet_models
channels:

  - defaults
dependencies:
    # Base depends
  - python >= 3.12
  - pip

  - numpy
  - pandas
  - scikit-learn
  - rich
  - intake
  - joblib
  - zarr >=3.0.0

  - rdkit
  - molfeat
  - datamol
  - mordredcommunity
  - lightgbm
  - seaborn
  - splito
  - umap-learn
  - modal
  - chemprop >= 2.1.1
  - pytorch-gpu
  - pytorch-lightning
  - cudatoolkit
  - wandb

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  - pip:
    - phx-class-registry
    - email-validator
